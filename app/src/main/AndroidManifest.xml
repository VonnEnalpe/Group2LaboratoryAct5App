<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.baru_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BARu_APP.">
        <activity android:name=".AUTHENTICATION.New_Device"></activity>
        <activity android:name=".Profile" />
        <activity android:name=".HomePage" />
        <activity android:name=".AUTHENTICATION.SendCredential" />
        <activity android:name=".AUTHENTICATION.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AUTHENTICATION.UserNotVerified" />
        <activity android:name=".AUTHENTICATION.RegisterPage" />
        <activity android:name=".Services" />
    </application>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>